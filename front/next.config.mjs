/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['www.apple.com', 'm.media-amazon.com', 'www.apple.com', 'm.media-amazon.com'],
  },
};

export default nextConfig;
